development:
  sessions:
    default:
      database: cfhack-development
      hosts:
        - localhost:27017

test:
  sessions:
    default:
      database: cfhack-test
      hosts:
        - localhost:27017



production:
  sessions:
    default:
      uri: <%= ENV["MONGO_URL"] %>

